# Tmux Configuration

Personal tmux configuration files.

## Files

- `tmux.conf` - Main tmux configuration
- `tmux-help` - Quick reference for keybindings

## Installation

```bash
make init
```

This symlinks `tmux.conf` to `~/.tmux.conf`.

## Help

Press `F1` within a tmux session to view keybinding reference.
